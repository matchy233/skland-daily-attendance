# 必填：森空岛凭据，多个用逗号分隔
SKLAND_TOKENS=your-token-1,your-token-2

# 可选：通知 URL，多个用逗号分隔
SKLAND_NOTIFICATION_URLS=your-notification-url

# 可选：最大重试次数 (默认为 3)
SKLAND_MAX_RETRIES=3

# 可选：跳过终末地签到（设置任意值即可启用；false/0/off/no 关闭）
# SKLAND_SKIP_ENDFIELD=true

# ===== 角色名显示设置 (可选) =====

# 可选：隐藏角色名（设置任意值即可启用；false/0/off/no 关闭）
# SKLAND_ANONYMOUS=true

# 可选：关闭昵称打码（设置任意值即可启用；false/0/off/no 关闭）
# SKLAND_NO_MASK=true

# ===== 持久化存储配置 (可选，选择其中一种) =====

# 可选：禁用持久化存储
# DISABLE_KV=false

# 选项 1: Upstash Redis (推荐用于 Serverless 环境)
# KV_REST_API_URL=https://your-upstash-redis.upstash.io
# KV_REST_API_TOKEN=your-token

# 或使用 Upstash 环境变量
# UPSTASH_REDIS_REST_URL=https://your-upstash-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

# 选项 2: Redis
# REDIS_URL=rediss://default:password@your-redis-host:6379

# 或使用通用 KV URL
# KV_URL=rediss://default:password@your-redis-host:6379

# 选项 3: AWS S3 兼容存储
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ENDPOINT=https://your-s3-endpoint.com
